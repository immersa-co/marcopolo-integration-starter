# Frontend
VITE_API_BASE_URL=http://localhost:8001

# Backend runtime
APP_ENV=development
APP_HOST=0.0.0.0
APP_PORT=8001
APP_CORS_ORIGINS=http://localhost:5173
API_BASE_PATH=/api
PUBLIC_API_BASE_URL=http://localhost:8001
FRONTEND_BASE_URL=http://localhost:5173

# Session
SESSION_SECRET=
SESSION_COOKIE_NAME=marcopolo_integration_demo_session
SESSION_MAX_AGE_SECONDS=43200
SESSION_HTTPS_ONLY=false

# The demo app-session form represents a user already authenticated by the
# partner application; it is not a real product login.
AUTH_REQUIRED=true
AUTH_DEFAULT_RETURN_URL=http://localhost:5173/

# Local MarcoPolo target used by the partner integration flow.
MARCOPOLO_MCP_URL=http://localhost:8000
MARCOPOLO_API_BASE_URL=http://localhost:8000/api
MARCOPOLO_WEB_BASE_URL=http://localhost:8000
MARCOPOLO_BROWSER_BOOTSTRAP_PATH=/api/auth/bootstrap
MARCOPOLO_BROWSER_BOOTSTRAP_REDIRECT=/app/

# Local-only shortcut; do not use this to validate partner authorization.
MARCOPOLO_DEVELOPER_API_TOKEN=

# Recommended partner flow: Entelligence WorkOS Standalone Connect.
WORKOS_API_BASE_URL=https://api.workos.com
WORKOS_API_KEY=
WORKOS_CONNECT_AUTH_URL=https://modern-berry-85-entelligence-integration.authkit.app
WORKOS_CONNECT_CLIENT_ID=client_01KXM9J1GK2R79E2Q53CMAQTZF
WORKOS_CONNECT_CLIENT_SECRET=
WORKOS_CONNECT_REDIRECT_URI=http://localhost:8001/api/auth/workos/callback
WORKOS_CONNECT_LOGIN_URI=http://localhost:8001/api/auth/workos/login
WORKOS_CONNECT_SCOPES=openid profile email offline_access
WORKOS_CONNECT_AUTHORIZE_PATH=/oauth2/authorize
WORKOS_CONNECT_TOKEN_PATH=/oauth2/token
WORKOS_CONNECT_COMPLETE_PATH=/authkit/oauth2/complete

# LLM runtime used by the Chatbot tab
LLM_PROVIDER=openai
LLM_MODEL=gpt-5-mini
LLM_API_BASE_URL=https://api.openai.com/v1
LLM_API_KEY=

# Clone the MarcoPolo plugin repo and point this at its skills directory:
# https://github.com/immersa-co/marcopolo-plugin
SKILL_REPO_PATH=
